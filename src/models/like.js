class Like {
    constructor(id, postId, userId, createdAt) {
        this.id = id;
        this.postId = postId;
        this.userId = userId;
        this.createdAt = createdAt;
    }
}

module.exports = Like;
